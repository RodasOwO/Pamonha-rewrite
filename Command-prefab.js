/*
const discord = require("discord.js");
exports.run = (Pamonha, message, args) => {
    try {

    } catch (err) {
        message.channel.send("não consegui executar o comando devido ao erro```" + err + "```")
    }

}
*/

//Coisas de db
/*{
    upsert: true,
    new: true,
  }
  */
